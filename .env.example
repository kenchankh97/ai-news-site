# =====================
# SERVER
# =====================
NODE_ENV=development
PORT=3000
APP_URL=http://localhost:3000

# =====================
# DATABASE (Railway PostgreSQL)
# Railway auto-injects DATABASE_URL in production
# =====================
DATABASE_URL=postgresql://user:password@host:5432/dbname

# =====================
# JWT SECRETS
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# =====================
JWT_ACCESS_SECRET=replace-with-64-char-random-hex-string
JWT_REFRESH_SECRET=replace-with-different-64-char-random-hex-string
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =====================
# SESSION (for CSRF protection and flash messages)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# =====================
SESSION_SECRET=replace-with-32-char-random-hex-string

# =====================
# GNEWS.IO (News API)
# Sign up free at https://gnews.io — 100 requests/day free
# =====================
GNEWS_API_KEY=your_gnews_api_key_here

# =====================
# OPENROUTER.AI (LLM for summarization + translation)
# Get your key at https://openrouter.ai
# Free model: qwen/qwen-2.5-72b-instruct:free
# =====================
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=qwen/qwen-2.5-72b-instruct:free
OPENROUTER_SITE_URL=https://your-app.up.railway.app
OPENROUTER_SITE_NAME=Your AI News

# =====================
# BREVO (Email — SMTP)
# Sign up free at https://brevo.com — 300 emails/day free
# Go to: Settings > SMTP & API > Generate SMTP Key
# =====================
BREVO_SMTP_USER=your_brevo_login_email@example.com
BREVO_SMTP_PASS=your_brevo_smtp_key_here
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=Your AI News

# =====================
# RATE LIMITING
# =====================
MANUAL_REFRESH_COOLDOWN_MINUTES=30
